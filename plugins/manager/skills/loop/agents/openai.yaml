interface:
  display_name: "WireNet Loop"
  short_description: "Run recurring checks in the current task"
  default_prompt: "Use $loop to keep checking this task until its stop condition is met."
policy:
  allow_implicit_invocation: true
